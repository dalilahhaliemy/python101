class Question:

    def __init__(self, prompt, answer):   # This function has 2 variable, answer and prompt
        self.prompt = prompt              # Defining input for prompt will be stored in prompt variable
        self.answer = answer              # Defining input for answer will be stored in answer variable
